{
  "name": "project-3",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd frontend && npm run dev\" \"cd backend && .\\venv\\Scripts\\activate\" \"cd backend && uvicorn main:app --reload\"",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.75.0",
    "autoprefixer": "^10.4.21",
    "axios": "^1.9.0",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.5",
    "concurrently": "^8.2.2"
  }
}
